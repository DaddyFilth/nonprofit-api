version: "3.9"

services:
  api:
    build: .
    container_name: nonprofit-api
    env_file:
      - .env
    ports:
      - "8001:8000"
    volumes:
      - ./reports:/app/reports
      - ./nonprofit.db:/app/nonprofit.db
    restart: unless-stopped
